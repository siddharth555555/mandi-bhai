PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=mandibhai
DB_PASSWORD=mandibhai_dev_pw
DB_NAME=mandibhai

# Dev-only default — replace before anything beyond local dev (see TODO.md)
JWT_SECRET=dev-only-secret-change-me
